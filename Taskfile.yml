---
# https://taskfile.dev
version: '3'
env :
  RENDER_DIR: .rendered

includes:
  # Vendored from datarobot-oss/cve-sync (taskfiles/cve-sync.yml). Only the
  # taskfile is vendored; the tool and the CVE policy are fetched at run time, so
  # this file does not need updating when a floor changes. Not flattened, so the
  # tasks are namespaced as `task cve-sync:<name>`.
  #
  # `task cve-sync:check-templates` is the one that matters most here: this repo's
  # real dependency surface is the pyproject.toml.jinja and the pre-baked locks
  # generated from it, and the plain `check` task cannot see either.
  cve-sync:
    taskfile: .taskfiles/cve-sync.yml

tasks:
  default:
    desc: "Show all available tasks"
    cmds:
      - task --list
    silent: true
  install:
    desc: "Install development dependencies for all agents into .venv_dev directory"
    cmds:
      - echo "Installing development dependencies for all agents"
      - uv sync --all-extras
      - mv pyproject.toml pyproject.toml.bak
      - uv run jinja -D agent_app_name DockerContext -D agent_template_framework docker 'template/{{ "{{agent_app_name}}" }}/pyproject.toml.jinja' -o pyproject.toml
      - UV_PROJECT_ENVIRONMENT=.venv_dev uv sync --all-extras
      - rm pyproject.toml
      - mv pyproject.toml.bak pyproject.toml
      - uv sync --all-extras

  install-e2e:
    desc: "Install dependencies needed to run E2E tests (prefers active venv when activated)"
    cmds:
      - echo "Installing E2E dependencies"
      - |
          if [ -n "${VIRTUAL_ENV:-}" ]; then
            uv sync --all-extras --active
          else
            uv sync --all-extras
          fi

  test-e2e:
    desc: "Install + run full deployment E2E"
    cmds:
      - task: install-e2e
      - task: e2e

  render-template:
    desc: "Render template"
    vars:
      USE_MEMORY: '{{.USE_MEMORY| default "none"}}'
      AGENT_TEMPLATE_FRAMEWORK: '{{ trimSuffix "_memory" .AGENT }}'
    cmds:
      - echo "Rendering template {{.AGENT}}"
      # Clean up the existing rendered directory except for .venv to speed up rendering and avoid conflict checks
      - cd ./$RENDER_DIR/agent_{{.AGENT}}/ && [ -d . ] && find . -mindepth 1 -maxdepth 1 ! -name '.venv' -exec rm -rf {} + || true
      - mkdir -p ./$RENDER_DIR/agent_{{.AGENT}}/.datarobot/answers/
      - cp ./fixtures/.datarobot/answers/llm-llm.yml ./$RENDER_DIR/agent_{{.AGENT}}/.datarobot/answers/llm-llm.yml
      - uvx copier copy . ./$RENDER_DIR/agent_{{.AGENT}} --defaults --data agent_template_framework={{.AGENT_TEMPLATE_FRAMEWORK}} --data use_agent_memory={{.USE_MEMORY}} --vcs-ref=HEAD
      - cp ./fixtures/Taskfile.yml ./$RENDER_DIR/agent_{{.AGENT}}/Taskfile.yml
      - mkdir -p ./$RENDER_DIR/agent_{{.AGENT}}/infra/infra
      - cp ./fixtures/infra/Taskfile.yml ./$RENDER_DIR/agent_{{.AGENT}}/infra/Taskfile.yml
      - cp ./fixtures/infra/pyproject.toml ./$RENDER_DIR/agent_{{.AGENT}}/infra/pyproject.toml
      - cp ./fixtures/infra/__init__.py ./$RENDER_DIR/agent_{{.AGENT}}/infra/infra/__init__.py
      - cp ./fixtures/infra/llm.py ./$RENDER_DIR/agent_{{.AGENT}}/infra/infra/llm.py

  render-template-e2e:
    desc: "Render template + inject test scaffolding used by E2E (Taskfiles + minimal infra entrypoint/files)"
    cmds:
      - task: render-template
        vars:
          AGENT: "{{.AGENT}}"
      # Add base component scaffolding used by our tests (Taskfiles + minimal infra entrypoint/files).
      - cp ./fixtures/Taskfile.yml ./$RENDER_DIR/agent_{{.AGENT}}/Taskfile.yml
      - mkdir -p ./$RENDER_DIR/agent_{{.AGENT}}/infra/infra
      - cp ./fixtures/infra/Taskfile.yml ./$RENDER_DIR/agent_{{.AGENT}}/infra/Taskfile.yml
      # Inject minimal infra scaffolding so `pulumi up` can run in this component repo.
      # (In a real AF project this is provided by the base/llm components.)
      - cp ./fixtures/infra/Pulumi.yaml ./$RENDER_DIR/agent_{{.AGENT}}/infra/Pulumi.yaml
      - cp ./fixtures/infra/__main__.py ./$RENDER_DIR/agent_{{.AGENT}}/infra/__main__.py
      - cp ./fixtures/infra/pyproject.toml ./$RENDER_DIR/agent_{{.AGENT}}/infra/pyproject.toml
      - cp ./fixtures/infra/__init__.py ./$RENDER_DIR/agent_{{.AGENT}}/infra/infra/__init__.py
      - cp ./fixtures/infra/llm.py ./$RENDER_DIR/agent_{{.AGENT}}/infra/infra/llm.py

  install-rendered:
    desc: "Install dev"
    cmds:
      - echo "Checking lock file for {{.AGENT}}"
      - uv lock --check --directory $RENDER_DIR/agent_{{.AGENT}}/agent
      - echo "Installing dependencies for {{.AGENT}}"
      - task agent:install --dir $RENDER_DIR/agent_{{.AGENT}}
      - task infra:install --dir $RENDER_DIR/agent_{{.AGENT}}

  render-install:
    desc: "Render template + install dependencies"
    cmds:
      - task: render-template
        vars:
          AGENT: "{{.AGENT}}"
          USE_MEMORY: '{{.USE_MEMORY| default "none"}}'
      - task: install-rendered
        vars:
          AGENT: "{{.AGENT}}"

  # datarobot-user-models carries more than one genai-agents drop-in environment,
  # each locked against a different Python. python311_genai_agents is the pinned
  # 3.11 one; python3_genai_agents ("Python 3") tracks the current Python and is
  # 3.13 today. Both are generated from the same template, so VERSION selects
  # which interpreter the render, the lock, and the requirements.txt are built
  # for. Get VERSION and AGENT_PATH out of step and you rewrite an environment's
  # requires-python and relock it against the wrong interpreter, so the task
  # echoes both before it touches anything.
  docker_update_reqs:
    desc: "Update a drop-in environment's pyproject.toml, uv.lock and requirements.txt from the jinja template (AGENT_PATH=<env dir> VERSION=3.11|3.13)"
    vars:
      # Defaults to 3.11 so the long-standing python311_genai_agents invocation
      # keeps working without a VERSION argument.
      PYTHON_VERSION: '{{.VERSION | default "3.11"}}'
    cmds:
      - |
        if [ -z "{{.AGENT_PATH}}" ]; then
          echo "AGENT_PATH is not set. Please set AGENT_PATH to the drop-in environment directory to update."
          echo "  task docker_update_reqs AGENT_PATH=/path/to/datarobot-user-models/public_dropin_environments/python311_genai_agents VERSION=3.11"
          echo "  task docker_update_reqs AGENT_PATH=/path/to/datarobot-user-models/public_dropin_environments/python3_genai_agents VERSION=3.13"
          exit 1
        fi
        if [ ! -d "{{.AGENT_PATH}}" ]; then
          echo "AGENT_PATH does not exist: {{.AGENT_PATH}}"
          exit 1
        fi
      - |
        # Keep this list in step with the version map in pyproject.toml.jinja. An
        # unlisted value renders a `requires-python = ">=X, <None"` that uv then
        # rejects with a confusing parse error, so reject it here instead.
        case "{{.PYTHON_VERSION}}" in
          3.11|3.12|3.13) ;;
          *)
            echo "VERSION={{.PYTHON_VERSION}} is not supported. Use one of: 3.11, 3.12, 3.13"
            echo "Adding a version means extending both this case and the version map at the top of template/{{"{{"}}agent_app_name{{"}}"}}/pyproject.toml.jinja."
            exit 1
            ;;
        esac
        echo "Updating {{.AGENT_PATH}} for Python {{.PYTHON_VERSION}}"
      - uv run jinja -D agent_app_name DockerContext -D agent_template_framework docker -D python_version {{.PYTHON_VERSION}} template/{{"{{"}}agent_app_name{{"}}"}}/pyproject.toml.jinja -o {{.AGENT_PATH}}/pyproject.toml
      - |
        # The template's `default("3.11")` is load-bearing for real copier runs, so
        # a dropped or misspelled `-D python_version` does not fail the render: it
        # silently emits 3.11. For python3_genai_agents that would relock against
        # the wrong interpreter and still look entirely plausible. The version map
        # only catches a value that is wrong, never one that never arrived, so
        # check the artifact rather than trusting the inputs.
        EXPECTED='requires-python = ">={{.PYTHON_VERSION}}, <'
        RENDERED=$(grep -m1 '^requires-python' "{{.AGENT_PATH}}/pyproject.toml")
        case "$RENDERED" in
          "$EXPECTED"*) echo "Render honors VERSION={{.PYTHON_VERSION}}: $RENDERED" ;;
          *)
            echo "Render did not honor VERSION={{.PYTHON_VERSION}}."
            echo "  expected to start with: $EXPECTED"
            echo "  got:                    $RENDERED"
            exit 1
            ;;
        esac
      - |
        # `uv lock` probes the project's .venv as an interpreter candidate and
        # hard-errors if it cannot query it, so a venv built against a different
        # Python blocks the whole task before resolution even starts. This bites
        # the first time an environment changes VERSION, and it bit
        # python3_genai_agents, whose directory was seeded by copying the 3.11 one
        # and so carried a 3.11 venv pointing at an interpreter that no longer
        # exists. The venv is a gitignored artifact that the `uv sync` below
        # recreates, so drop it instead of making the operator work it out from a
        # dyld error about a missing libpython dylib.
        VENV="{{.AGENT_PATH}}/.venv"
        if [ -n "{{.AGENT_PATH}}" ] && [ -d "$VENV" ] && \
           ! "$VENV/bin/python" -c "import sys; sys.exit(0 if '.'.join(map(str, sys.version_info[:2])) == '{{.PYTHON_VERSION}}' else 1)" 2>/dev/null; then
          echo "Removing $VENV: not a working Python {{.PYTHON_VERSION}} environment"
          rm -rf "$VENV"
        fi
      # --no-upgrade on purpose. Packages move when datarobot-genai bumps them or a
      # cve-sync constraint/override forces them higher, never just because a newer
      # release exists: the agentic packages iterate fast enough that a free
      # re-resolve is a reliable way to break the environment.
      - cd {{.AGENT_PATH}} && uv lock --python={{.PYTHON_VERSION}} --no-upgrade && uv sync --python={{.PYTHON_VERSION}}
      # This command is temporary. It is using pip compile to produce a truncated requirements.txt without dependencies to avoid overloading the HHI installer.
      - cd {{.AGENT_PATH}} && uv pip compile --python={{.PYTHON_VERSION}} --python-platform=x86_64-manylinux_2_28 --no-header --no-annotate --no-deps --extra agentic_playground --output-file=requirements.txt pyproject.toml
      # Need to extend support for larger requirements.txt files to enable this.
      # - cd {{.AGENT_PATH}} && uv pip freeze > {{.AGENT_PATH}}/requirements.txt
      # - cat {{.AGENT_PATH}}/requirements.txt

  docker_build_local:
    desc: "Build docker image locally (Check container builds)"
    cmds:
      - cd template/{{"{{"}}agent_app_name{{"}}"}}/docker_context && docker build -f Dockerfile . -t test --platform linux/amd64

  test-agent:
    desc: "Run tests"
    cmds:
      - task: render-install
        vars:
          AGENT: "{{.AGENT}}"
          USE_MEMORY: '{{.USE_MEMORY| default "none"}}'
      - echo "Running tests for {{.AGENT}}"
      - cd $RENDER_DIR/agent_{{.AGENT}} && echo "LLM_DEFAULT_MODEL='datarobot/azure/gpt-5-mini-2025-08-07'" >> .env
      - |
          if [ "{{.SKIP_LINT}}" != "1" ]; then
            task agent:lint-check --dir $RENDER_DIR/agent_{{.AGENT}}
          else
            echo "Skipping lint check for agent: {{.AGENT}} because SKIP_LINT=1"
          fi
      - task agent:test-coverage --dir $RENDER_DIR/agent_{{.AGENT}}
      - |
          if [ "{{.SKIP_INFRA}}" != "1" ]; then
            task infra:lint-check --dir $RENDER_DIR/agent_{{.AGENT}}
            task infra:test-coverage --dir $RENDER_DIR/agent_{{.AGENT}}
          else
            echo "Skipping lint check for agent: {{.AGENT}} because SKIP_INFRA=1"
          fi

  test-base:
    desc: "Run tests for base agent"
    cmds:
      - task: test-agent
        vars:
          AGENT: base
          SKIP_LINT: "{{.SKIP_LINT| default 0}}"
          SKIP_INFRA: "{{.SKIP_INFRA| default 0}}"

  test-memory:
    desc: "Run tests for base agent with Mem0 memory enabled"
    cmds:
      - task: test-agent
        vars:
          AGENT: base_memory
          USE_MEMORY: mem0
          SKIP_LINT: "{{.SKIP_LINT| default 0}}"
          SKIP_INFRA: "{{.SKIP_INFRA| default 0}}"

  test-datarobot-memory:
    desc: "Run tests for base agent with DataRobot memory enabled"
    cmds:
      - task: test-agent
        vars:
          AGENT: base_memory
          USE_MEMORY: datarobot_memory_service
          SKIP_LINT: "{{.SKIP_LINT| default 0}}"
          SKIP_INFRA: "{{.SKIP_INFRA| default 0}}"

  test-nat:
    desc: "Run tests for nat agent"
    cmds:
      - task: test-agent
        vars:
          AGENT: nat
          SKIP_LINT: "{{.SKIP_LINT| default 0}}"
          SKIP_INFRA: "{{.SKIP_INFRA| default 0}}"

  test-crewai:
    desc: "Run tests for crewai agent"
    cmds:
      - task: test-agent
        vars:
          AGENT: crewai
          SKIP_LINT: "{{.SKIP_LINT| default 0}}"
          SKIP_INFRA: "{{.SKIP_INFRA| default 0}}"

  test-langgraph:
    desc: "Run tests for langgraph agent"
    cmds:
      - task: test-agent
        vars:
          AGENT: langgraph
          SKIP_LINT: "{{.SKIP_LINT| default 0}}"
          SKIP_INFRA: "{{.SKIP_INFRA| default 0}}"

  test-llamaindex:
    desc: "Run tests for llamaindex agent"
    cmds:
      - task: test-agent
        vars:
          AGENT: llamaindex
          SKIP_LINT: "{{.SKIP_LINT| default 0}}"
          SKIP_INFRA: "{{.SKIP_INFRA| default 0}}"

  validate-windows-compatibility:
    desc: "Validate that file paths don't contain characters illegal on Windows"
    cmds:
      - cmd: |
          echo "Scanning for Windows-incompatible file names (<>:\"|?*)..."
          total=$(find ./template -type f | wc -l | tr -d ' ')
          failures=$(find ./template -type f | while IFS= read -r f; do
            if echo "$f" | grep -q '[<>:"|?*]'; then
              echo "FAIL: $f" >&2
              echo "$f"
            else
              echo "  ok: $f" >&2
            fi
          done)
          bad=0
          if [ -n "$failures" ]; then
            bad=$(echo "$failures" | wc -l | tr -d ' ')
            echo ""
            echo "Offending files:"
            echo "$failures"
          fi
          echo ""
          echo "Checked $total files, $bad incompatible."
          [ "$bad" -gt 0 ] && exit 1 || true
        platforms: [linux, darwin]

  test:
    desc: "Run tests for all agents"
    cmds:
      - task: validate-windows-compatibility
      - task: test-base
      - task: test-memory
      - task: test-datarobot-memory
      - task: test-crewai
      - task: test-langgraph
      - task: test-llamaindex
      - task: test-nat
      # CLI tests fail because it runs auth-check
      # - task: test-cli
      # - task: test-cli-json
      # - task: test-cli-streaming

  update-lock-file:
    desc: "Update lock file for {{.AGENT}}"
    cmds:
      - echo "Updating lock file for {{.AGENT}}"
      - task: render-template
        vars:
          AGENT: "{{.AGENT}}"
          USE_MEMORY: '{{.USE_MEMORY| default "none"}}'
      - |
          if [ "$UPGRADE_LOCK" = "1" ]; then
            uv lock -U --directory $RENDER_DIR/agent_{{.AGENT}}/agent
          else
            uv lock --directory $RENDER_DIR/agent_{{.AGENT}}/agent
          fi
      - cat $RENDER_DIR/agent_{{.AGENT}}/agent/uv.lock | sed 's/^name = "agent"$/name = "{{`{{agent_app_name}}`}}"/' > template/{{`{{agent_app_name}}`}}/uvlock_templates/uvlock_{{.AGENT}}.j2

  update-lock-file-all:
    desc: "Update lock file for all agents"
    cmds:
      - task: update-lock-file
        vars:
          AGENT: "base"
      - task: update-lock-file
        vars:
          AGENT: "base_memory"
          USE_MEMORY: "mem0"
      - task: update-lock-file
        vars:
          AGENT: "crewai"
      - task: update-lock-file
        vars:
          AGENT: "langgraph"
      - task: update-lock-file
        vars:
          AGENT: "llamaindex"
      - task: update-lock-file
        vars:
          AGENT: "nat"

  run-cli:
    desc: "Run CLI"
    cmds:
      - task: render-install
        vars:
          AGENT: "base"
      - echo "Running tests for {{.AGENT}}"
      - cd $RENDER_DIR/agent_base && echo "DATAROBOT_API_TOKEN=secret" > .env && echo "DATAROBOT_ENDPOINT=https://test.com/api/v2" >> .env
      - task agent:cli --dir $RENDER_DIR/agent_base -- execute --user_prompt "Artificial Intelligence" > $RENDER_DIR/agent_base/agent/output.log 2>&1

  test-cli:
    desc: "Test CLI"
    cmds:
      - task: run-cli
      - task: check-output

  check-output:
      # These checks always run against the base agent, whose LLM call is a mock
      # and emits no NAT intermediate LLM-token steps, so DRAgent's console
      # frontend takes the non-streaming path and prints "Workflow Result:".
      # (Streaming frameworks like langgraph instead print "Run finished." with no
      # result line - see .github/workflows/afcomponentagent-smoke-test.yaml.)
      - if [ $(wc -l < $RENDER_DIR/agent_base/agent/output.log) -lt 13 ]; then exit 1; fi
      - if ! cat $RENDER_DIR/agent_base/agent/output.log | grep -q "Running CLI execute"; then exit 1; fi
      - if ! cat $RENDER_DIR/agent_base/agent/output.log | grep -q "Running agent with user prompt"; then exit 1; fi
      - 'if ! cat $RENDER_DIR/agent_base/agent/output.log | grep -q "Workflow Result:"; then exit 1; fi'

  run-cli-streaming:
    desc: "Run CLI with Streaming"
    cmds:
      - task: render-install
        vars:
          AGENT: "base"
      - echo "Running tests for {{.AGENT}}"
      - cd $RENDER_DIR/agent_base && echo "DATAROBOT_API_TOKEN=secret" > .env && echo "DATAROBOT_ENDPOINT=https://test.com/api/v2" >> .env
      - task agent:cli --dir $RENDER_DIR/agent_base -- execute --user_prompt "Artificial Intelligence" > $RENDER_DIR/agent_base/agent/output.log 2>&1

  test-cli-streaming:
    desc: "Test CLI with Streaming"
    cmds:
      - task: run-cli-streaming
      - task: check-output

  build-docker:
    desc: "Build docker image"
    cmds:
      - task: render-install
        vars:
          AGENT: "{{.AGENT}}"
      - echo "Building docker image for {{.AGENT}}"
      - task agent:build-docker --dir $RENDER_DIR/agent_{{.AGENT}}

  build-docker-base:
    desc: "Build docker for base agent"
    cmds:
      - task: build-docker
        vars:
          AGENT: base

  build-docker-crewai:
    desc: "Build docker for crewai agent"
    cmds:
      - task: build-docker
        vars:
          AGENT: crewai

  build-docker-langgraph:
    desc: "Build docker for langgraph agent"
    cmds:
      - task: build-docker
        vars:
          AGENT: langgraph

  build-docker-llamaindex:
    desc: "Build docker for llamaindex agent"
    cmds:
      - task: build-docker
        vars:
          AGENT: llamaindex

  build-docker-nat:
    desc: "Build docker image for NAT"
    cmds:
      - task: build-docker
        vars:
          AGENT: nat

  e2e:
    desc: "Run full deployment E2E for all agents (requires DATAROBOT_ENDPOINT + DATAROBOT_API_TOKEN)"
    cmds:
      - echo "Running full deployment E2E (all agent frameworks)"
      - |
          # Load local repo `.env` (gitignored) if present.
          # Keep it scoped to E2E so credentials don't leak into unrelated tasks.
          set -a
          if [ -f ".env" ]; then
            . ".env"
          fi
          set +a

          if [ -n "${VIRTUAL_ENV:-}" ]; then
            RUN_E2E=1 uv run --all-extras --active pytest --import-mode=importlib -vv -s ./tests/e2e/test_agent_e2e.py
          else
            RUN_E2E=1 uv run --all-extras pytest --import-mode=importlib -vv -s ./tests/e2e/test_agent_e2e.py
          fi
